services:
  api:
    build: .
    env_file:
      - .env
    expose:
      - "9080"
    volumes:
      - search-leads-data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/search_leads.db
    restart: unless-stopped

volumes:
  search-leads-data: